# Source Trace Manifest

This manifest records the source routines that the next emitted binary should
port first. It is the working contract for source-guided emission: read the C,
then write Python emitter routines that generate equivalent x86 machine code.

## Stage: source_stage01_wad_map

Output:

```text
build/source_stage01_wad_map.exe
```

Scope:

- Runtime WAD directory loading.
- Runtime lump lookup.
- Runtime loading of the core map setup lumps needed before BSP rendering.
- On-screen count/sanity output, not a renderer yet.

| Source routine | Source file | Starts at | Emitted label | Status | Validation target |
| --- | --- | ---: | --- | --- | --- |
| `W_NumLumps` | `reference/chocolate-doom/src/w_wad.c` | 249 | `wad_num_lumps` | emitted | Returns the loaded runtime directory count. |
| `W_CheckNumForName` | `reference/chocolate-doom/src/w_wad.c` | 261 | `wad_check_num_for_name` | emitted | Uses the no-hash-table backwards scan path and finds `MAP01`, returning -1 for missing names. |
| `W_GetNumForName` | `reference/chocolate-doom/src/w_wad.c` | 310 | `wad_get_num_for_name` | emitted | Calls `wad_check_num_for_name`; missing names set a visible load-failure path. |
| `W_LumpLength` | `reference/chocolate-doom/src/w_wad.c` | 329 | `wad_lump_length` | emitted | Reports the selected directory entry byte size. |
| `W_ReadLump` | `reference/chocolate-doom/src/w_wad.c` | 346 | `wad_read_lump` | emitted | Seeks and reads the selected lump into the emitted data arena, checking the byte count. |
| `P_LoadVertexes` | `reference/chocolate-doom/src/doom/p_setup.c` | 120 | `map_load_vertexes` | emitted | Converts signed 16-bit map coordinates to 16.16 fixed. |
| `P_LoadSectors` | `reference/chocolate-doom/src/doom/p_setup.c` | 275 | `map_load_sectors` | emitted | Converts floor/ceiling heights to 16.16 fixed and keeps flat names, light, special, and tag fields. |
| `P_LoadSideDefs` | `reference/chocolate-doom/src/doom/p_setup.c` | 495 | `map_load_sidedefs` | emitted | Preserves offsets, texture names, sector indexes, and sector links. |
| `P_LoadLineDefs` | `reference/chocolate-doom/src/doom/p_setup.c` | 414 | `map_load_linedefs` | emitted | Builds vertex links, dx/dy, bbox, slopetype, side indexes, and sector links. |

## Important Source Ordering

`P_SetupLevel` loads these map structures in this order:

```text
P_LoadBlockMap
P_LoadVertexes
P_LoadSectors
P_LoadSideDefs
P_LoadLineDefs
P_LoadSubsectors
P_LoadNodes
P_LoadSegs
P_GroupLines
P_LoadReject
P_LoadThings
```

The first source-guided stage intentionally stops after `P_LoadLineDefs`. That
keeps the slice small while preserving the real dependency order: linedefs need
loaded vertexes, sidedefs, and sectors.

## Notes From Initial Read

- `W_CheckNumForName` scans backwards when no lump hash table exists. The first
  emitted stage can use that linear path before implementing `W_GenerateHashTable`.
- `W_LumpLength` and `W_ReadLump` are good early ports because their behavior is
  small but they exercise the real lump directory layout.
- `P_LoadVertexes` is the cleanest first map conversion routine: each 4-byte map
  vertex becomes two 32-bit fixed-point coordinates.
- `P_LoadLineDefs` should come after sectors and sidedefs because it stores
  front/back sector pointers derived through sidedef indexes.

## source_stage01 Implementation Notes

- The emitted path is generated by `tools/emit_source_stage01_wad_map.py`.
- The executable reads the full WAD directory into `lump_directory` at runtime;
  `wad_check_num_for_name` uses the same backwards linear scan Chocolate Doom
  uses before its lump hash table exists.
- Map lump selection follows the `ML_*` offsets from `doomdata.h`, matching
  the `P_SetupLevel` order for `MAP01`.
- The first slice keeps texture and flat names as raw 8-byte names because the
  texture/flat managers are later source-guided slices.

## Stage: source_stage02_bsp_setup

Output:

```text
build/source_stage02_bsp_setup.exe
```

Scope:

- Reuse the stage01 runtime WAD directory and core map lump loaders.
- Load and convert the `SSECTORS`, `NODES`, and `SEGS` lumps for `MAP01`.
- Build enough of `P_GroupLines` to assign subsector sectors and validate
  per-sector line grouping with counts, starts, and a line pointer table.
- Report deterministic BSP setup counts and sanity values in the window title
  and framebuffer text.

| Source routine | Source file | Starts at | Emitted label | Status | Validation target |
| --- | --- | ---: | --- | --- | --- |
| `P_LoadSubsectors` | `reference/chocolate-doom/src/doom/p_setup.c` | 246 | `map_load_subsectors` | emitted | Converts `mapsubsector_t` records into runtime `subsector_t` fields: sector pointer placeholder, numlines, and firstline. |
| `P_LoadNodes` | `reference/chocolate-doom/src/doom/p_setup.c` | 308 | `map_load_nodes` | emitted | Converts node partition fields and bounding boxes to 16.16 fixed point and preserves child indexes. |
| `P_LoadSegs` | `reference/chocolate-doom/src/doom/p_setup.c` | 174 | `map_load_segs` | emitted | Builds seg vertex, linedef, sidedef, frontsector, and backsector links from the loaded stage01 structures. |
| `P_GroupLines` | `reference/chocolate-doom/src/doom/p_setup.c` | 567 | `map_group_lines` | emitted | Assigns subsector sectors, counts sector line references, builds sector line starts/work counts, and fills a grouped line pointer table. |

## source_stage02 Implementation Notes

- The emitted path is generated by `tools/emit_source_stage02_bsp_setup.py`.
- The output filename contains `setup`, so the PE writer embeds a minimal
  `asInvoker` manifest resource to avoid Windows installer-elevation
  heuristics while preserving the required executable name.
- `P_GroupLines` intentionally stops before blockmap-derived sector block boxes
  because blockmap loading is outside this slice. The releasable proof is the
  source-ordered subsector sector assignment and sector line grouping.
- The pinned `third_party\freedoom\freedoom2.wad` `MAP01` smoke signal is:
  `V=1189 L=1274 SD=2041 SEC=211 SS=698 N=697 SG=2233 ROOT=696 G=3..81 F0=16`.

## Stage: source_stage03_bsp_walk_debug

Output:

```text
build/source_stage03_bsp_walk_debug.exe
```

Scope:

- Reuse the stage02 WAD/map/BSP setup.
- Add source-guided BSP front-to-back traversal from the pinned `MAP01` player
  start.
- Render a simple top-down debug view and report traversal counters.
- Keep `R_CheckBBox` as an explicit accept-all debug boundary; the real
  bounding-box visibility routine is planned for stage04.

| Source routine | Source file | Starts at | Emitted label | Status | Validation target |
| --- | --- | ---: | --- | --- | --- |
| `R_PointOnSide` | `reference/chocolate-doom/src/doom/r_main.c` | 145 | `render_point_on_side` | emitted | Synthetic node classifications match source branch behavior, including vertical/horizontal partition fast paths; pinned traversal matches reference order. |
| `R_PointInSubsector` | `reference/chocolate-doom/src/doom/r_main.c` | 793 | `render_point_in_subsector` | emitted | Fixed `MAP01` player start resolves to subsector `227`, the first visited subsector in front-to-back order. |
| `R_Subsector` | `reference/chocolate-doom/src/doom/r_bsp.c` | 496 | `render_debug_subsector` | emitted debug adaptation | Counts and records visited subsectors/segs without planes, sprites, `R_AddLine`, or wall drawing. |
| `R_RenderBSPNode` | `reference/chocolate-doom/src/doom/r_bsp.c` | 555 | `render_bsp_node_debug` | emitted debug adaptation | Traverses the real BSP in source front-to-back order. `R_CheckBBox` is an explicit accept-all debug boundary in this stage. |

## source_stage03 Implementation Notes

- The emitted path is generated by `tools/emit_source_stage03_bsp_walk_debug.py`.
- The executable reuses the stage02 WAD/map/BSP setup and seeds `viewx`,
  `viewy`, and `viewangle` from the pinned `MAP01` player-one start
  `(-192, -192, 0)`.
- `render_point_on_side` ports the source vertical/horizontal fast paths,
  sign-bit shortcut, and fixed multiply comparison used by Doom BSP ordering.
- `render_bsp_node_debug` follows source recursion order. The back child is
  always traversed through `render_check_bbox_accept_all`, documenting the
  stage03 boundary before real `R_CheckBBox` visibility in stage04.
- `render_debug_subsector` is intentionally a counter/debug adaptation: it
  records visited seg indexes for a top-down framebuffer highlight and leaves
  planes, sprites, `solidsegs`, and wall columns untouched.
- The pinned `third_party\freedoom\freedoom2.wad` `MAP01` smoke signal is:
  `V=1189 SEC=211 SD=2041 L=1274 SS=698 N=697 SG=2233 VN=697 VSS=698 VSEG=2233 DEPTH=33 FIRSTSS=227 LASTSS=169`.

## Stage: source_stage04_bbox_visibility_debug

Output:

```text
build/source_stage04_bbox_visibility_debug.exe
```

Scope:

- Replace stage03's accept-all bbox shortcut with source-guided
  view-frustum/bounding-box visibility.
- Add the angle/projection table data needed by `R_CheckBBox`.
- Initialize `solidsegs` with only the source sentinel ranges from
  `R_ClearClipSegs`; real wall-span updates are planned for stage05.
- Continue to avoid `R_AddLine`, texture columns, planes, sprites, and full
  wall rendering.

| Source routine | Source file | Starts at | Emitted label | Status | Validation target |
| --- | --- | ---: | --- | --- | --- |
| `SlopeDiv` | `reference/chocolate-doom/src/tables.c` | 41 | `render_slope_div` | emitted | Threshold and clamping behavior match source for `R_PointToAngle` slope indexes. |
| `tantoangle` / `finetangent` | `reference/chocolate-doom/src/tables.c` | 64 / 1867 | `render_angle_tables` | emitted/table-emitted | Selected entries match the pinned Chocolate Doom tables used by `R_PointToAngle` and `R_InitTextureMapping`. |
| `R_PointToAngle` | `reference/chocolate-doom/src/doom/r_main.c` | 275 | `render_point_to_angle` | emitted | Octant tests and unsigned `angle_t` wraparound match the source-shaped Python reference. |
| `R_InitTextureMapping` | `reference/chocolate-doom/src/doom/r_main.c` | 539 | `render_init_texture_mapping_tables` | emitted/table-emitted | Selected `viewangletox`, `xtoviewangle`, and `clipangle` values match source-equivalent table generation for `viewwidth=320`. |
| `R_ClearClipSegs` | `reference/chocolate-doom/src/doom/r_bsp.c` | 244 | `render_clear_clipsegs` | emitted | Initializes only `solidsegs[0] = [-0x7fffffff, -1]`, `solidsegs[1] = [viewwidth, 0x7fffffff]`, and `newend = solidsegs + 2`. |
| `R_CheckBBox` | `reference/chocolate-doom/src/doom/r_bsp.c` | 380 | `render_check_bbox` | emitted | Pinned `MAP01` sentinel-only bbox traversal reports `BVN=559 BVSS=513 BVSEG=1709 BDEPTH=33 BFIRSTSS=227 BLASTSS=153 CULL=47`. |

## source_stage04 Implementation Notes

- The emitted path is generated by
  `tools/emit_source_stage04_bbox_visibility_debug.py`.
- The executable reuses the stage03 accept-all traversal as a baseline pass,
  then runs a second BSP traversal where only the back-child decision calls
  source-shaped `render_check_bbox`.
- `render_clear_clipsegs` intentionally initializes only the two source
  sentinel ranges. `solidsegs` is not mutated by wall spans in this stage.
- `render_angle_tables` table-emits Chocolate Doom `finetangent` and
  `tantoangle` data from `tables.c`; `render_init_texture_mapping_tables`
  table-emits `viewangletox`, `xtoviewangle`, and `clipangle` generated from
  the source `R_InitTextureMapping` algorithm for `viewwidth=320`.
- The top-down framebuffer overlays bbox-visible segs over the accept-all
  traversal and leaves planes, sprites, texture columns, `R_AddLine`,
  `R_ClipSolidWallSegment`, and `R_ClipPassWallSegment` out of scope.
- The pinned `third_party\freedoom\freedoom2.wad` `MAP01` smoke signal is:
  `V=1189 SEC=211 SD=2041 L=1274 SS=698 N=697 SG=2233 VN=697 VSS=698 VSEG=2233 DEPTH=33 FIRSTSS=227 LASTSS=169 BVN=559 BVSS=513 BVSEG=1709 BDEPTH=33 BFIRSTSS=227 BLASTSS=153 CULL=47`.

## Stage: source_stage05_seg_clip_debug

Output:

```text
build/source_stage05_seg_clip_debug.exe
```

Scope:

- Reuse stage04 angle tables, `R_PointToAngle`, bbox visibility, and
  `R_ClearClipSegs`.
- Freeze a Python source-shaped reference that calls debug `R_AddLine` from
  bbox-visible subsectors.
- Update reference `solidsegs` through the source-shaped solid/pass wall
  clipping routines.
- Let reference mutable `solidsegs` feed later `R_CheckBBox` decisions during
  the same clipping pass.
- Record accepted wall column spans in the reference instead of building full
  drawsegs or drawing textured wall columns.

| Source routine | Source file | Starts at | Emitted label | Status | Validation target |
| --- | --- | ---: | --- | --- | --- |
| `R_Subsector` | `reference/chocolate-doom/src/doom/r_bsp.c` | 496 | `render_debug_subsector_clip` | reference/status proof | Python reference sets `frontsector`, iterates real subsector segs, and validates pinned mutable traversal totals: `CLN=72 CLSS=56 CLSEG=205 CLCULL=17`; live emitted x86 is planned for stage06. |
| `R_AddLine` | `reference/chocolate-doom/src/doom/r_bsp.c` | 258 | `render_add_line_debug` | reference/status proof | Python reference maps seg endpoints through `R_PointToAngle`/`viewangletox`, rejects backfaces/off-frustum/zero-pixel spans, classifies solid/pass/empty trigger lines, and freezes pinned counters: `BF=82 OFF=17 ZPX=5 SOL=30 PASS=70 EMPTY=1`; live emitted x86 is planned for stage06. |
| `R_ClipSolidWallSegment` | `reference/chocolate-doom/src/doom/r_bsp.c` | 103 | `render_clip_solid_wall_segment` | reference validated | Python source-shaped reference tests cover insert, front extend, tail extend, and merge behavior; pinned `MAP01` ends with one covering solidseg range. |
| `R_ClipPassWallSegment` | `reference/chocolate-doom/src/doom/r_bsp.c` | 196 | `render_clip_pass_wall_segment` | reference validated | Python source-shaped reference tests prove pass spans record visible fragments without mutating `solidsegs`. |
| `R_StoreWallRange` | `reference/chocolate-doom/src/doom/r_segs.c` | 372 | `render_store_wall_range_debug` | reference/status proof | Python reference records debug-only `start..stop`, reason, and source seg index; pinned `MAP01` stores 86 spans, first `224..255 seg 605`, last `143..165 seg 855`; live emitted x86 is planned for stage06. |

## source_stage05 Implementation Notes

- The emitted path is generated by
  `tools/emit_source_stage05_seg_clip_debug.py`.
- Stage05 keeps the source_stage04 accept-all and sentinel-only bbox-visible
  passes as comparison baselines, then reports the pinned mutable clipping
  reference totals in the executable title/status.
- The emitted stage05 executable does not yet compute the clipping pass live in
  x86; this is an explicit planning correction for stage06, not a projection
  starting point.
- The Python reference runs the source-shaped mutable clipping traversal before
  PE emission and is frozen in tests. It starts with `R_ClearClipSegs`, uses
  `R_CheckBBox` against mutable `solidsegs`, adapts `R_Subsector` to call
  debug `R_AddLine`, and adapts `R_StoreWallRange` to record spans only.
- The debug span record layout is `start`, `stop`, `reason`, and `seg_index`;
  no drawseg projection, texture lookup, planes, sprites, masked textures, or
  later-stage routines are included.
- The pinned `third_party\freedoom\freedoom2.wad` `MAP01` smoke signal keeps
  the stage04 baselines and adds:
  `CLN=72 CLSS=56 CLSEG=205 CLCULL=17 BF=82 OFF=17 ZPX=5 SOL=30 PASS=70 SPAN=86 NSEGS=1`.

## Stage: source_stage06_live_seg_clip_debug

Output:

```text
build/source_stage06_live_seg_clip_debug.exe
```

Scope:

- Replace stage05's frozen clipping counters with a live emitted x86 mutable
  wall-span traversal.
- Keep the stage04 accept-all and sentinel-only bbox baselines.
- Run `R_ClearClipSegs`, live clipping-pass BSP traversal, live debug
  `R_AddLine`, live solid/pass clipping, and live debug span recording.
- Still avoid projection, `R_PointToDist`, `R_ScaleFromGlobalAngle`, texture
  lookup, `R_RenderSegLoop`, `R_DrawColumn`, visplanes, masked textures, and
  sprites.

| Source routine | Source file | Starts at | Emitted label | Status | Validation target |
| --- | --- | ---: | --- | --- | --- |
| `R_RenderBSPNode` | `reference/chocolate-doom/src/doom/r_bsp.c` | 555 | `render_bsp_node_clip_debug` | emitted debug adaptation | Matches the stage05 Python reference traversal while using mutable `solidsegs` for later `R_CheckBBox` calls. |
| `R_Subsector` | `reference/chocolate-doom/src/doom/r_bsp.c` | 496 | `render_debug_subsector_clip` | emitted live x86 | Sets `frontsector`, iterates real segs, and calls live debug `R_AddLine`; pinned run reports `CLSS=56 CLSEG=205`. |
| `R_AddLine` | `reference/chocolate-doom/src/doom/r_bsp.c` | 258 | `render_add_line_debug` | emitted live x86 | Computes angle clipping, x spans, backface/off-frustum/zero-pixel rejects, and solid/pass/empty classification from runtime data: `BF=82 OFF=17 ZPX=5 SOL=30 PASS=70 EMPTY=1`. |
| `R_ClipSolidWallSegment` | `reference/chocolate-doom/src/doom/r_bsp.c` | 103 | `render_clip_solid_wall_segment` | emitted live x86 | Mutates `solidsegs`, records visible fragments, and reports insert/extend/merge counters; pinned `MAP01` ends with one covering solidseg range. |
| `R_ClipPassWallSegment` | `reference/chocolate-doom/src/doom/r_bsp.c` | 196 | `render_clip_pass_wall_segment` | emitted live x86 | Records visible fragments without mutating `solidsegs`; participates in the pinned `SPAN=86` debug range total. |
| `R_StoreWallRange` | `reference/chocolate-doom/src/doom/r_segs.c` | 372 | `render_store_wall_range_debug` | emitted live x86 debug adaptation | Records `start`, `stop`, reason, and source seg index only; smoke anchors are first `224..255 seg 605` and last `143..165 seg 855`. |

## source_stage06 Implementation Notes

- The emitted path is generated by
  `tools/emit_source_stage06_live_seg_clip_debug.py`.
- Stage06 keeps the source_stage04 accept-all and sentinel-only bbox-visible
  passes as comparison baselines, then runs a third mutable clipping pass in
  emitted x86 from `R_ClearClipSegs`.
- `render_bsp_node_clip_debug` reuses the live `render_check_bbox`, so
  mutations made by `render_clip_solid_wall_segment` feed later back-child
  visibility checks during the same traversal.
- The debug span buffer layout is still `start`, `stop`, `reason`, and
  `seg_index`, bounded to 512 records. The executable reports first and last
  span anchors in the title to prove runtime buffer writes:
  `FSPAN=224-255 FSEG=605 LSPAN=143-165 LSEG=855`.
- The pinned `third_party\freedoom\freedoom2.wad` `MAP01` smoke signal keeps
  the stage04 baselines and reports live clipping totals:
  `CLN=72 CLSS=56 CLSEG=205 CLCULL=17 BF=82 OFF=17 ZPX=5 SOL=30 PASS=70 SPAN=86 NSEGS=1`.
- This stage still does not include `R_PointToDist`,
  `R_ScaleFromGlobalAngle`, drawseg projection, `R_RenderSegLoop`,
  `R_DrawColumn`, texture lookup, visplanes, sprites, masked textures, or
  source_stage07 work.

## Stage: source_stage07_wall_projection_debug

Output:

```text
build/source_stage07_wall_projection_debug.exe
```

Scope:

- Reuse stage06 live clipped wall spans.
- Add stable fixed-player frame setup fields, including a documented fixed
  `viewz` seed from the source view-height path.
- Port Doom fixed division, distance, and scale math for accepted wall ranges.
- Still avoid texture lookup, `R_RenderSegLoop`, `R_DrawColumn`, visplanes,
  masked textures, and sprites.

| Source routine | Source file | Starts at | Emitted label | Status | Validation target |
| --- | --- | ---: | --- | --- | --- |
| `finesine` / `finecosine` | `reference/chocolate-doom/src/tables.c` | 581 / 1865 | `render_fine_trig_tables` | emitted/table-emitted | Selected sine/cosine entries and quarter-wave offset match Chocolate Doom tables. |
| `VIEWHEIGHT` / `P_CalcHeight` viewz path | `reference/chocolate-doom/src/doom/p_local.h` / `reference/chocolate-doom/src/doom/p_user.c` | 31 / 70 | `render_setup_frame_debug` | emitted source-read adaptation | Fixed debug `viewz` is seeded as pinned sector floor plus Doom `VIEWHEIGHT`, matching the stable no-bob start path before real `player_t`/`mobj_t` setup exists. |
| `R_SetupFrame` | `reference/chocolate-doom/src/doom/r_main.c` | 822 | `render_setup_frame_debug` | emitted debug adaptation | Seeds fixed-player view state, `viewz`, `viewcos`, `viewsin`, `validcount`, and `framecount` without requiring real player/mobj setup yet. |
| `FixedDiv` | `reference/chocolate-doom/src/m_fixed.c` | 47 | `render_fixed_div` | emitted | Signed fixed division and overflow saturation match source behavior for `R_PointToDist` and `R_ScaleFromGlobalAngle`. |
| `R_PointToDist` | `reference/chocolate-doom/src/doom/r_main.c` | 375 | `render_point_to_dist` | emitted | Synthetic distances and pinned seg endpoint distances match the source-shaped Python reference. |
| `R_ScaleFromGlobalAngle` | `reference/chocolate-doom/src/doom/r_main.c` | 448 | `render_scale_from_global_angle` | emitted | Clamp/min/max behavior and selected accepted span scales match source-shaped calculations. |
| `R_StoreWallRange` distance/scale prefix | `reference/chocolate-doom/src/doom/r_segs.c` | 372 | `render_store_wall_range_projected_debug` | emitted debug adaptation | Records the unchanged debug span plus `rw_distance`, `rw_normalangle`, `scale1`, `scale2`, and `scalestep`, then stops before texture, plane, masked texture, sprite, or column work. |

## source_stage07 Implementation Notes

- The emitted path is generated by
  `tools/emit_source_stage07_wall_projection_debug.py`.
- Stage07 preserves the stage04 accept-all/bbox baselines and the stage06 live
  clipping oracle unchanged:
  `CLN=72 CLSS=56 CLSEG=205 CLCULL=17 BF=82 OFF=17 ZPX=5 SOL=30 PASS=70 SPAN=86 NSEGS=1`,
  with first/last spans `224..255 seg 605` and `143..165 seg 855`.
- `render_setup_frame_debug` is a fixed-player adaptation. It uses the current
  `R_PointInSubsector` result to read the pinned start sector floor, then seeds
  `viewz = floorheight + 41*FRACUNIT`, `viewcos`, `viewsin`, `validcount`, and
  `framecount`.
- `render_fine_trig_tables` table-emits Chocolate Doom `finesine`; the
  `render_finecosine_table` label is placed at the source quarter-wave offset
  `finesine[FINEANGLES/4]`.
- `render_store_wall_range_projected_debug` keeps the stage06 16-byte debug
  span writes and adds a 32-byte projection record:
  `x1`, `x2`, source seg index, `rw_normalangle`, `rw_distance`, `scale1`,
  `scale2`, and `scalestep`.
- The pinned `third_party\freedoom\freedoom2.wad` `MAP01` projection smoke
  signal is:
  `VZ=2686976 VCOS=65535 VSIN=25 VALID=1 FRAME=1 PRJ=86 MIND=2073560 MAXD=58720255 MINS=11702 MAXS=108495 FPRJ=224-255 FPSEG=605 LPRJ=143-165 LPSEG=855`.
- This stage still does not include texture lookup, `R_RenderSegLoop`,
  `R_DrawColumn`, visplanes, sprites, masked textures, light tables, or
  source_stage08 work.

## Stage: source_stage08_texture_data_setup_debug

Output:

```text
build/source_stage08_texture_data_setup_debug.exe
```

Scope:

- Reuse the stage07 live clipping and wall projection debug pipeline.
- Parse `PNAMES`, `TEXTURE1`, and optional `TEXTURE2` into bounded
  source-shaped texture metadata at emission time, then table-emit those
  layouts into the PE.
- Emit texture translation, width-mask, height, column-lump,
  column-offset, composite-size, and flat translation metadata sufficient for
  the next renderer slice.
- Resolve MAP01 sidedef top/bottom/mid texture names and sector floor/ceiling
  flat names to numeric IDs.
- Replace the raw `"-"` empty-line shortcut in live `R_AddLine` with the
  source-shaped `midtexture == 0` check, while preserving all clipping and
  projection counters.
- Still avoid texture pixel lookup, column drawing, colormaps, light tables,
  visplanes, sprites, masked textures, and `source_stage09`.

| Source routine | Source file | Starts at | Emitted label | Status | Validation target |
| --- | --- | ---: | --- | --- | --- |
| `R_InitTextures` | `reference/chocolate-doom/src/doom/r_data.c` | 448 | `render_init_texture_data_setup_debug` | emitted/table-emitted metadata | Parses and validates `PNAMES`, `TEXTURE1`, optional `TEXTURE2`, patch refs, texture dimensions, patch counts, `texturetranslation`, `texturewidthmask`, and `textureheight`; pinned Freedoom2 reports `TEX=963 PN=1054`. |
| `R_GenerateLookup` metadata/column-directory portion | `reference/chocolate-doom/src/doom/r_data.c` | 292 | `render_generate_lookup_metadata_debug` | emitted/table-emitted metadata | Counts direct patch-backed columns and later composite columns without reading/drawing texture pixels; pinned Freedoom2 reports `DIRC=80797 COMPC=26323`. |
| `R_InitFlats` | `reference/chocolate-doom/src/doom/r_data.c` | 625 | `render_init_flats_debug` | emitted/table-emitted metadata | Resolves `firstflat`, `lastflat`, `numflats`, and `flattranslation`; pinned Freedoom2 reports `FLAT=246`. |
| `R_CheckTextureNumForName` | `reference/chocolate-doom/src/doom/r_data.c` | 736 | `render_check_texture_num_for_name_debug` | source-shaped Python reference and emitted table consumer | `"-"` returns `0`, missing names return `-1`, hash chains preserve first texture wins, and case-insensitive lookup resolves MAP01 sidedefs. |
| `R_TextureNumForName` | `reference/chocolate-doom/src/doom/r_data.c` | 767 | `render_texture_num_for_name_debug` | source-shaped Python reference and emitted table consumer | Resolves every MAP01 sidedef top/bottom/mid texture name, with `6123` texture references validated. |
| `R_FlatNumForName` | `reference/chocolate-doom/src/doom/r_data.c` | 712 | `render_flat_num_for_name_debug` | source-shaped Python reference and emitted table consumer | Resolves every MAP01 sector floor/ceiling flat name, with `422` flat references validated. |
| `P_LoadSideDefs` texture IDs | `reference/chocolate-doom/src/doom/p_setup.c` | 495 | `map_load_sidedefs_texture_ids_debug` | emitted/table-emitted resolution | Keeps raw stage07 sidedef names available and emits parallel numeric ID tables for top/bottom/mid texture fields; live `R_AddLine` consumes `midtexture == 0`. |
| `P_LoadSectors` flat IDs | `reference/chocolate-doom/src/doom/p_setup.c` | 275 | `map_load_sectors_flat_ids_debug` | emitted/table-emitted resolution | Keeps raw stage07 sector names available and emits parallel numeric floor/ceiling flat ID tables for source-shaped empty-line classification. |

## source_stage08 Implementation Notes

- The emitted path is generated by
  `tools/emit_source_stage08_texture_data_setup_debug.py`.
- Stage08 keeps the stage07 live clipping and projection pipeline intact:
  `CLN=72 CLSS=56 CLSEG=205 CLCULL=17 BF=82 OFF=17 ZPX=5 SOL=30 PASS=70 SPAN=86 NSEGS=1`
  and
  `PRJ=86 MIND=2073560 MAXD=58720255 MINS=11702 MAXS=108495`.
- The pinned texture/flat setup smoke signal is
  `TEX=963 PN=1054 FLAT=246 DIRC=80797 COMPC=26323 FPTEX=850 LPTEX=13 EMID=1`.
- `FPTEX=850` resolves the first projected span to `AQRUST08`; `LPTEX=13`
  resolves the last projected span to `BIGDOOR1` through the source-shaped
  sidedef/sector metadata decision boundary used before `R_RenderSegLoop`.
- The output filename contains `setup`, so the PE includes an `asInvoker`
  manifest resource to avoid Windows installer-elevation heuristics.
- This stage still does not include `R_RenderSegLoop`, `R_DrawColumn`,
  `R_GetColumn`, `R_GenerateComposite`, `R_DrawColumnInCache`,
  `R_InitColormaps`, `R_InitLightTables`, visplanes, sprites, masked
  textures, or `source_stage09`.

## Stage: source_stage09_direct_wall_column_pixels_debug

Output:

```text
build/source_stage09_direct_wall_column_pixels_debug.exe
```

Scope:

- Reuse stage08 live clipping/projection, texture IDs, flat IDs, and direct vs
  composite column metadata.
- Parse direct patch column posts, `PLAYPAL`, and `COLORMAP` row 0 at emission
  time, then table-emit only the direct opaque columns reached by the pinned
  fixed view.
- Draw the first real WAD wall pixels through an emitted `R_DrawColumn`-shaped
  scaler into the existing 320x200 32-bit framebuffer.
- Count and skip texture id `0`, composite-needed columns, unsupported
  two-sided/non-opaque spans, and masked midtextures.
- Still avoid composite generation, two-sided upper/lower wall edges, plane
  spans, actors, sky, movement, and gameplay.

| Source routine/data | Source file | Starts at | Emitted label | Status | Validation target |
| --- | --- | ---: | --- | --- | --- |
| `R_GetColumn` direct path | `reference/chocolate-doom/src/doom/r_data.c` | 377 | `render_get_column_direct_debug` | emitted direct-data adaptation | Synthetic wrapping tests prove `col & texturewidthmask`; pinned run skips `135` composite-needed columns and draws only direct patch-backed columns. |
| `R_DrawColumn` | `reference/chocolate-doom/src/doom/r_draw.c` | 102 | `render_draw_column_debug` | emitted narrow scaler | Synthetic stepping tests prove `dc_texturemid + (dc_yl-centery)*dc_iscale`, `&127` source indexing, RGB lookup, and runtime pixel signature updates. |
| `R_StoreWallRange` one-sided midtexture setup | `reference/chocolate-doom/src/doom/r_segs.c` | 372 | `render_direct_wall_column_pixels_debug` | emitted/table-fed debug adaptation | Pinned fixed view considers `86` projected spans, finds `24` one-sided opaque midtexture spans, and attempts `297` direct candidate columns. |
| `R_RenderSegLoop` midtexture branch | `reference/chocolate-doom/src/doom/r_segs.c` | 199 | `render_direct_wall_column_pixels_debug` | emitted narrow debug loop | Draws `162` direct columns and `15508` real WAD pixels while preserving all stage08 counters. |
| `patch_t` / `post_t` column data | `reference/chocolate-doom/src/v_patch.h` | 29 / 40 | `stage09_direct_patch_column_decode_debug` | source-shaped Python parse/table emission | Synthetic post tests validate patch headers, post traversal, and full-height opaque direct-column filtering. |
| `PLAYPAL` / `COLORMAP` row 0 | WAD graphics data | n/a | `stage09_palette32` | table-emitted fixed-colormap adaptation | Converts Doom palette indices to deterministic 32-bit RGB values; pinned runtime pixel signature is `2194105880`. |

## source_stage09 Implementation Notes

- The emitted path is generated by
  `tools/emit_source_stage09_direct_wall_column_pixels_debug.py`.
- Stage09 keeps the stage08 clipping/projection and texture setup smoke signal
  unchanged:
  `CLN=72 CLSS=56 CLSEG=205 CLCULL=17 BF=82 OFF=17 ZPX=5 SOL=30 PASS=70 SPAN=86 NSEGS=1`,
  `PRJ=86 MIND=2073560 MAXD=58720255 MINS=11702 MAXS=108495`, and
  `TEX=963 PN=1054 FLAT=246 DIRC=80797 COMPC=26323 FPTEX=850 LPTEX=13 EMID=1`.
- The direct pixel smoke signal is:
  `DWSP=86 OPQSP=24 DCOL=297 DRAW=162 SKC=135 SKU=62 ZTEX=0 MASK=0 FTEX=850 FN=AQRUST08 FCOL=127 PIX=15508 SIG=2194105880`.
- `R_GetColumn` is narrowed to the direct patch-backed branch. Columns whose
  stage08 `texturecolumnlump` entry is not a positive patch lump are counted as
  composite-needed and skipped; no composite cache is built.
- `R_DrawColumn` writes 32-bit RGB values through a table-emitted fixed
  colormap adaptation using `COLORMAP` row 0 and the first `PLAYPAL` palette.
- The first drawn texture is `AQRUST08` (`850`), column `127`, from the first
  projected span. The last projected texture still resolves to `BIGDOOR1`
  (`13`) but is not drawn because its columns need composites.
- This stage still does not include composite generation, two-sided wall edge
  drawing, plane spans, actors, sky, movement, or gameplay.

## Stage: source_stage10_composite_two_sided_wall_edges_debug

Output:

```text
build/source_stage10_composite_two_sided_wall_edges_debug.exe
```

Scope:

- Reuse stage09 direct wall pixels, fixed `PLAYPAL`/`COLORMAP` RGB adaptation,
  and emitted `R_DrawColumn`-shaped scaler.
- Add source-shaped `R_DrawColumnInCache`, `R_GenerateComposite`, and
  direct/composite `R_GetColumn` reference dispatch over real patch post data.
- Preserve stage09 direct counters while classifying the 135 stage09
  composite-needed midtexture columns into drawn or explicitly clipped/skipped
  outcomes.
- Add a source-shaped two-sided upper/lower edge pass using initialized
  `ceilingclip[320]` and `floorclip[320]`, plus plane-mark records for the
  stage11 handoff.
- Still avoid flat drawing, sky drawing, masked wall drawing, sprites, actors,
  movement, gameplay, and `source_stage11`.

| Source routine/data | Source file | Starts at | Emitted label | Status | Validation target |
| --- | --- | ---: | --- | --- | --- |
| `R_DrawColumnInCache` | `reference/chocolate-doom/src/doom/r_data.c` | 93 | `render_draw_column_in_cache_source_shape_debug` | source-shaped Python reference/table emission | Synthetic tests cover `originy` clipping, cache height clipping, and overlapping patch order. |
| `R_GenerateComposite` | `reference/chocolate-doom/src/doom/r_data.c` | 133 | `render_generate_composite_cache_debug` | source-shaped Python reference/table emission | Composite cache is keyed by `(texture, column)` and reports pinned `CMB=89 CMH=75 CMO=0`. |
| `R_GetColumn` direct/composite dispatch | `reference/chocolate-doom/src/doom/r_data.c` | 377 | `render_get_column_direct_or_composite_debug` | emitted table-fed debug adaptation | Direct columns return direct post bytes; composite columns build/reuse cached bytes; bad/missing/overflow columns are counted. |
| `R_DrawColumn` | `reference/chocolate-doom/src/doom/r_draw.c` | 102 | `render_draw_column_debug` | emitted narrow scaler | Draws all stage10 direct/composite/edge commands and updates runtime `TCOL`, `TPIX`, and `TSIG`. |
| `R_StoreWallRange` upper/lower setup | `reference/chocolate-doom/src/doom/r_segs.c` | 372 | `render_two_sided_wall_edges_debug` | source-shaped Python reference/table emission | Pinned `SKU=62` set resolves to `30` supported edge spans, `478` upper columns, and `138` lower columns. |
| `R_RenderSegLoop` top/bottom branches | `reference/chocolate-doom/src/doom/r_segs.c` | 199 | `render_two_sided_wall_edges_debug` | source-shaped Python reference/table emission | Synthetic tests cover `ceilingclip`, `floorclip`, `pixhigh`, and `pixlow` clipping. |
| `R_ClearPlanes` / `R_CheckPlane` hooks | `reference/chocolate-doom/src/doom/r_plane.c` | 114 / 181 | `render_plane_mark_records_debug` | record/count debug adaptation | Records `727` ceiling marks and `932` floor marks; no plane pixels are drawn. |

## source_stage10 Implementation Notes

- The emitted path is generated by
  `tools/emit_source_stage10_composite_two_sided_wall_edges_debug.py`.
- Stage10 keeps the stage09 title signal unchanged:
  `DWSP=86 OPQSP=24 DCOL=297 DRAW=162 SKC=135 SKU=62 ZTEX=0 MASK=0 FTEX=850 FN=AQRUST08 FCOL=127 PIX=15508 SIG=2194105880`.
- The stage10 wall-pixel smoke signal is:
  `CMB=89 CMH=75 CMO=0 MCOL=2 MCEMP=133 UCOL=478 UCOMP=6 LCOL=138 PM=1659 F10TEX=850 F10N=AQRUST08 L10TEX=887 L10N=AQSECT08 TCOL=780 TPIX=37546 TSIG=4201955800`.
- The one-sided pass intentionally preserves stage09's direct proof shape, then
  routes composite-needed columns through the source-shaped composite cache.
  Of the `135` composite-needed midtexture columns, `2` are visible in the
  fixed proof and `133` are explicitly counted as clipped empty after lookup.
- The two-sided edge pass initializes source-shaped clipping arrays and records
  plane marks, but it table-emits the selected column bytes and does not call
  `R_DrawPlanes`.
- `R_GetColumn` remains a debug/table-fed adaptation in emitted x86: Python
  performs the source-shaped composite cache construction for the pinned WAD,
  and the executable draws those emitted column bytes while proving the runtime
  `R_DrawColumn` counters and signature.
